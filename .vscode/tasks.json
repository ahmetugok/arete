{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Deploy to gh-pages",
			"type": "shell",
			"command": "CI=false npm run build && npx gh-pages -d build",
			"isBackground": false,
			"group": "build"
		}
	]
}